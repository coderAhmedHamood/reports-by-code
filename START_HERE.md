# 🎯 ابدأ من هنا - نظام بناء التقارير الديناميكي

## ⚡ التشغيل السريع (3 خطوات)

### 1️⃣ تثبيت المكتبات

**على macOS/Linux:**
```bash
./INSTALL.sh
```

**على Windows:**
```cmd
INSTALL.bat
```

**أو يدوياً:**
```bash
npm install
```

### 2️⃣ تشغيل المشروع
```bash
npm run dev
```

### 3️⃣ فتح المتصفح
افتح: **http://localhost:3000**

---

## 📚 الملفات التوثيقية

| الملف | الوصف |
|------|-------|
| **[QUICK_START.md](./QUICK_START.md)** | دليل البدء السريع (3 خطوات) |
| **[SETUP.md](./SETUP.md)** | دليل التثبيت والتشغيل الكامل |
| **[USAGE.md](./USAGE.md)** | دليل الاستخدام المفصل |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | البنية المعمارية للنظام |
| **[README.md](./README.md)** | نظرة عامة على المشروع |

---

## ✅ التحقق من التثبيت

بعد تشغيل `npm run dev`، يجب أن ترى:

```
✓ Ready in 2.5s
○ Local:        http://localhost:3000
```

إذا رأيت هذا، فكل شيء يعمل! 🎉

---

## 🎨 استخدام سريع

1. **اختر قالب** من الصفحة الرئيسية
2. **أدخل البيانات** في لوحة البيانات (يمين)
3. **عدّل الأقسام** بالضغط عليها
4. **صدّر PDF** بالضغط على الزر الأزرق

---

## ⚠️ مشاكل شائعة وحلولها

### ❌ المنفذ 3000 مستخدم؟

**macOS/Linux:**
```bash
lsof -ti:3000 | xargs kill -9
```

**Windows:**
```cmd
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

**أو استخدم منفذ آخر:**
```bash
PORT=3001 npm run dev
```

### ❌ npm install فشل؟

```bash
# تنظيف الكاش
npm cache clean --force

# حذف وإعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### ❌ أخطاء TypeScript؟

```bash
npm install --save-dev @types/node @types/react @types/react-dom
```

---

## 📦 المتطلبات

- ✅ Node.js 18.x أو أحدث
- ✅ npm أو yarn
- ✅ متصفح حديث (Chrome, Firefox, Safari, Edge)

---

## 🚀 الأوامر المتاحة

```bash
npm run dev      # تشغيل خادم التطوير
npm run build    # بناء للإنتاج
npm start        # تشغيل النسخة الإنتاجية
npm run lint     # فحص الأخطاء
```

---

## 📞 الدعم

- راجع ملفات التوثيق أعلاه
- تحقق من قسم "استكشاف الأخطاء" في SETUP.md
- راجع أمثلة البيانات في `examples/sample-data.json`

---

## 🎯 الخطوات التالية

1. ✅ تثبيت المكتبات
2. ✅ تشغيل المشروع
3. ✅ فتح المتصفح
4. ✅ اختيار قالب وإنشاء تقرير تجريبي
5. ✅ تصدير PDF والتحقق من النتيجة

---

**آخر تحديث**: 2024  
**الإصدار**: 1.0.0

