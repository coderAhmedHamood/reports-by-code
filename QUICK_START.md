# دليل البدء السريع

## 🚀 التشغيل في 3 خطوات

### 1️⃣ تثبيت المكتبات
```bash
npm install
```

### 2️⃣ تشغيل المشروع
```bash
npm run dev
```

### 3️⃣ فتح المتصفح
افتح: **http://localhost:3000**

---

## ✅ التحقق من نجاح التثبيت

بعد تشغيل `npm run dev`، يجب أن ترى:

```
✓ Ready in 2.5s
○ Local:        http://localhost:3000
```

إذا رأيت هذا، فكل شيء يعمل بشكل صحيح! 🎉

---

## 📝 استخدام سريع

1. **اختر قالب** من الصفحة الرئيسية
2. **أدخل البيانات** في لوحة البيانات (يمين)
3. **عدّل الأقسام** بالضغط عليها
4. **صدّر PDF** بالضغط على الزر الأزرق

---

## ⚠️ مشاكل شائعة

### المنفذ 3000 مستخدم؟
```bash
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# أو استخدم منفذ آخر
PORT=3001 npm run dev
```

### npm install فشل؟
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 للمزيد من التفاصيل

راجع ملف **SETUP.md** للدليل الكامل

